// 화살표 함수
// add, minus
const add = (a, b) => {
  return a + b;
};

const minus = (a, b) => {
  return a - b;
};
// = const minus = (a,b) => a- b;

//홀짝
const oddeven = (x) => (x % 2 ? "홀" : "짝");
//const oddeven = (x) => (x % 2 ==0 ? "짝" : "홀");
